default_language_version:
  python: python3.9

default_stages: [commit, push]

repos:
  - repo: local
    hooks:
      - id: check-codestyle
        name: check-codestyle
        entry: make check-codestyle
        language: system
